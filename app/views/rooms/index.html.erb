<div class="container">
  <div class="row">
    <div class="col-sm-10 com-sm-offset-1">
      <h1>Habitaciones</h1>

      <%= link_to "Nueva Habitación", new_room_path, class: "btn btn-primary" %>
      <% @rooms.each do |room| %>
        <div class="room">
          <%= room.title %>
        </div>
      <% end %>
    </div>
  </div>
</div>